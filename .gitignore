node_modules/
.env

.vercel
.env*

# RAG module (Python) — rag/chroma_db/ is committed on purpose: it's the
# pre-built index the Vercel deployment bundles into api/ask.py (see README).
rag/__pycache__/
rag/**/__pycache__/
*.pyc
